import React from 'react';
//import { Link } from 'react-router-dom';
//import { Route } from 'react-router-dom'

const SearchButton = (props) => {
  return (
    <div className="open-search">
      <button>
        {/* <Link to={`/dialog/chat/${props.user}`}> */}
        {/* </Link> */}
      </button>
    </div>
  )
}

export default SearchButton;
